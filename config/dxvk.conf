# ============================================================
#  dxvk.conf — FexDXVK NonAdreno v3.0.2
#  Optimized for Mali / PowerVR GPUs on ARM (via FEX-Emu)
# ============================================================

# Async shader compilation and persistent state cache
dxvk.enableAsync = True
dxvk.numCompilerThreads = 0
dxvk.enableStateCache = True
dxvk.stateCachePath = ""
dxvk.shaderModel = 6

# Persistent pipeline cache
dxvk.enableGraphicsPipelineLibrary = True
dxvk.pipelineCompilerOptimizations = True

# Command buffer chunk size (moderate avoids oversized Mali allocations)
dxvk.maxChunkSize = 2

# Frame pacing
dxvk.presentInterval = 1
dxvk.latencySleep = True
dxvk.latencySleepDelay = 0
dxvk.maxFrameLatency = 2

# Non-Adreno driver fixes
dxvk.invariantPosition = True
dxvk.zeroWorkgroupMemory = False
dxvk.nativeUnorderedAccessViews = True
dxvk.useRawSsbo = False

# Avoid aggressive pipeline parallelism on tile-based mobile GPUs
dxvk.enableGraphicsPipelineLibrary = True
dxvk.pipelineCompilerOptimizations = True

# D3D9
d3d9.maxFrameRate = 0
d3d9.presentInterval = 1
d3d9.numBackBuffers = 2
d3d9.numFramesInFlight = 2
d3d9.strictPow = True

# D3D11
d3d11.maxFrameLatency = 1
d3d11.numBackBuffers = 2
d3d11.syncInterval = 1
d3d11.maxFeatureLevel = 11_0

# DXVK HUD is enabled by the wrapper only when monitoring is requested.

# Logging (set to "debug" for troubleshooting)
dxvk.logLevel = warn
dxvk.logPath = ""
